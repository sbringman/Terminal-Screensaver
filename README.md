# Terminal-Screensaver
A set of programs to run from the terminal that display something interesting while idling in the background.

### Game of Life
A Python program to run Conway's Game of Life on the terminal screen. Use the command `python engine.py` to run the script. Command line arguments can be viewed with `python engine.py -h`.
